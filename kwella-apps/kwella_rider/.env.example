# Copy this file to .env (git-ignored) and fill in your real key.
# Google Maps Platform key with the "Places API" enabled — can reuse the
# same key configured for Maps SDK in android/local.properties /
# ios/Flutter/Secrets.xcconfig as long as Places API is enabled on it.
GOOGLE_PLACES_API_KEY=
